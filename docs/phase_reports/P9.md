# Phase 9 — Recognition / LoopClosing / MapMerging (2026-08-09~10)

## 범위와 결과

LoopClosing(2496줄)+KeyFrameDatabase를 정찰(3-agent, `docs/P9_RECON.md`) →
5단계 커밋으로 정리했다. 탐지 상태머신을 타입드+트레이스드 구조로 캡슐화하고
PlaceRecognition을 분리했으며, 상속 결함 7건(D1~D7)의 처분을 확정했다.

| 커밋 | 내용 | 단계 검증 |
|---|---|---|
| 4aca34c P9-1 | 사장 코드 전수 삭제: 레거시 ORB-SLAM2 탐지기 상태 일체, 매크로 밖 타이밍 벡터, KFDB 폐기 detect 3종+직렬화 배관, KeyFrame 죽은 필드 2개, 죽은 public mpViewer/mpORBVocabulary. −963줄. 적대적 diff 재검증 ALL CLEAN(8항목) | 빌드, 비트 PASS, 스모크 0.0304, KITTI 07 루프 발화 |
| a6b685f P9-2 | 문서화: OWNERSHIP.md에 LC 레이스 R-a~f(이중 뮤텍스 큐, 무락 GBA 스폰 플래그 등) + GBA 스레드 수명 기록 + SetNotErase 래치 프로토콜/누수 L1·L2/리셋 미소거 D5. DIVERGENCES #21(오염 소비 합성, R-1 형상 — FixLevel 1순위)·#24(raw mbFixScale) 보존 기재 | 문서 전용 |
| 86aab9c P9-3 | 안전 가드 2건: #22 GBA 완주 스레드 reap(중단 경로 포인터 널링 동반 — 가드 자체가 UAF가 되지 않도록), #23 KFDB bad-KF 무한루프 행 가드(반복자 전진 보장) | 빌드, 스모크 0.0466, KITTI 07 |
| cf86070 P9-4 | **타입드 탐지 상태머신**: 쌍둥이 멤버 8쌍 → DetectionChannel ×2, 전이는 명명 헬퍼 경유 + stderr 트레이스 9종 이벤트. 사이트별 적대적 검증 **ALL-VERBATIM 10/10** — 하중 비대칭 4건(#21) 보존, BoW 미스 경로 no-op 증명, 트레이스 무간섭 확인 | 빌드, 비트 PASS, 스모크 0.0362, KITTI 07 트레이스 확인 |
| d8218d3 P9-5 | **PlaceRecognition 추출**(P8-4 mHost 패턴): 탐지 5함수+머신 이동, 큐 변형은 전부 LoopClosing 잔류(PopNewKeyFrame), 소비측은 const 접근자+명명 와이프 4종 깔때기. 자구 증명 **단일 헌크**(팝 블록→호출) | 빌드, 비트 PASS, 스모크 0.0406, KITTI 07 0.51m |

관측성 수확: KITTI 07에서 상태머신 트레이스가 실전 검증됨 —
`bow-seed cnt=3`(단일 KF 공간 검증으로 즉시 DETECTED) → `wipe-consume`.
래치 누수 L1/L2와 리셋 미소거(D5)는 이제 트레이스로 가시화된다(수정은 FixLevel).

## 게이트 판정

풀 게이트 v2.2.1, 4라운드, KITTI 00 (`benchmark/gate_runs/20260809_231549`):

```
euroc_mono                 0.0184(4)   0.0203(4)  +10.4%  0.657 PASS
euroc_mono_inertial        0.0760(4)   0.0690(4)   -9.2%  0.829 PASS
euroc_stereo               0.0473(4)   0.0377(4)  -20.3%  0.986 PASS
euroc_stereo_inertial      0.0399(4)   0.0404(4)   +1.3%  0.557 PASS
kitti_stereo               1.1859(4)   1.1849(4)   -0.1%  0.557 PASS
FULL GATE: PASS
```

**#20 재계측 (사전 계획된 SI 상시 8v8)**: 게이트 직후 보충 4쌍을 이어 붙여
8v8 판정 — **orig 0.0438 vs new 0.0404, Δ=−7.7%, p=0.677: 청정**.
P8에서 계측된 +13.5%(p=0.033) 상승이 P9 팁에서는 관측되지 않는다. 해석:
레이아웃/타이밍 매개 효과가 P9의 코드 재배치로 다시 이동했거나(같은 효과
계급의 왕복), P8 측정에 잔여 일간 성분이 있었거나 — 어느 쪽이든 #20의 재개
조건("추가 악화")은 발동하지 않았고, 절대 수치도 역대 대역 안이다.
KITTI 00(-0.1%)은 루프클로징 전 경로(탐지→보정→GBA)가 P9 수술 후에도 원본과
등가임을 보여주는 핵심 판정이다.

## 이 게이트가 증명하지 않는 것

- 다중 맵 병합 경로(MergeLocal/MergeLocal2)는 게이트 시나리오에서 미발현 —
  #21 오염 소비, mvpMergeConnectedKFs 누적, MergeLocal2의 교차 스레드
  UpdateFrameIMU는 여전히 실전 미검증 (FixLevel/P10 대상)
- #13(LLT 강건성) 미반증 상태 지속 — chi2 브래키팅 무경고가 간접 증거
- LC 레이스 R-a~f는 문서화만 됨(P10)

## 방법론 수확

- **SI 상시 8v8이 정착**: 경계 p값을 기다리지 않고 처음부터 보충 쌍을 게이트에
  이어 붙이는 자동화(p9_gate.sh 패턴)로 #20 의무를 이행 — 판정 지연 없음.
- 자구 증명 파이프라인(접두 제거 diff)이 2회째 재사용됨(P8-4 → P9-5). 추출
  커밋의 표준 검증으로 승격할 가치.
- 적대적 사이트별 검증(P9-4 ALL-VERBATIM 10/10)이 상태머신 개편의 신뢰 기반 —
  특히 "미스 경로 쓰기 no-op 증명" 같은 비자명 등가를 문서로 남김.

## 백로그 이월

- R-a~f + R1-R5 + Shutdown GBA 방치/SaveAtlas 경쟁 + CorrectLoop EmptyQueue
  순서(R2) → **P10**
- FixLevel 후보 축적분: #9, #21, L1/L2, #13, D5 리셋 소거 → **P11**
- mvpMergeConnectedKFs 로컬화(동작 변화라 보류), MergeLocal2 AddMergeEdge
  부재 비대칭 → P11 FixLevel 검토 시 함께
- DBoW2 바이너리 어휘 로드 패치(1안 확정, 2026-08-10) → **P11**
