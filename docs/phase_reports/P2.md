# Phase 2 리포트 — 기반 계층 리팩토링 (2026-08-06 ~ 08-07)

## 산출물 (커밋 4994283 → e397714)

- **P2-0**: 59개 파일을 12개 모듈 디렉터리로 재배치(.hpp/.cpp 정규화, git mv 이력 보존)
- **P2-1**: 죽은 Config 클래스 골격 삭제
- **P2-2**: 설정 파싱 단일화 — Tracking 레거시 Parse* 3함수(~810줄)와 무호출
  ChangeCalibration 삭제, File.version 1.0 필수화, 레거시 전용 파라미터
  (IMU.fastInit·Camera.imageScale·loopClosing)를 Settings로 흡수,
  mnFramesToResetIMU 미초기화 UB 제거(명시 =0)
- **P2-3**: Converter 315→129줄(죽은 17함수, g2o::SE3Quat 변환 전부 제거),
  Verbose를 core/Verbose.hpp로 분리
- Lie 유틸 double/float 통합은 P6(수치 등가 테스트 이후)로 의도적 연기

## 풀 게이트 v2 판정 전말

교차 짝지음 18회: 4모드 즉시 PASS(kitti는 1.2087 vs 1.2088로 사실상 동일).
euroc_mono가 n=4에서 p=0.014로 FAIL → 3단계 커밋 이등분 + 3-agent 적대적
diff 리뷰로 추적한 결과:

- 세 커밋(P2-0/1, P2-2, P2-3) 모두 **직접 짝지음 재현에서 깨끗** — P2-2/P2-3
  빌드가 전체 최고 기록(0.0138/0.0127)을 경신하며 원본과 완전히 겹침
- diff 리뷰: 단안 경로 의미 변화 0건 (오버로드 하이재킹·found 플래그·ODR 전수 검사)
- 원인 판정: 머신 상태가 악화된 시간창(90분 게이트 직후)에 신규 실행이 몰린
  표집 불운 + 반복 검정 다중 비교. 야간 장시간 세션에서 mono 분산 폭발 실측
  (원본조차 0.0152→0.0294 표류)

**최종: PASS**

## 방법론 부록 (게이트 v2.1)

1. 풀 게이트는 휴지 상태 머신에서(장시간 연속 벤치마크 직후 금지)
2. 유의 판정 시 즉시 커밋 이등분 재현 실험으로 확증 — 단발 p값으로 단정 금지
   (오늘 밤 ~10회 검정 중 1회 p=0.014는 다중 비교상 기대 범위)
3. mono는 최고분산 모드 — mono 단독 FAIL은 재현 필수
