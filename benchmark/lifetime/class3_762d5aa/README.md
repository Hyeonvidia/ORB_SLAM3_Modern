# 수명 이벤트 분포 — 클래스 3 캡처 (Optimizer 62사이트 @ 762d5aa)

캡처 2026-08-12, /build/lifetime 트리(v2 락 프리 레저), L2 mono-inertial
MH01 · L3 kitti 00. 62사이트 전수 계측(sites.txt), 발화 6사이트.
섭동 게이트: 쌍 동일성은 모드 스래시로 불충족, **2단계(골든 우주 포함)
판정 PASS — 양 팔 신규 행동 0건** (lifetime_perturb.sh 티어-2 주석 참조).

## 처분 (클래스 3)

| 사이트군 | 실측 | 처분 |
|---|---|---|
| **OptLBA.1290** (LBA가 살아있는 MP의 관측 맵을 순회하며 만나는 죽은 KF) | L3: 479객체 × **12,109회**, 중앙값 3.2s, 최대 **512s** | **내력벽** — MapPreSave.obsErase와 동일 가족(비대칭 erase가 남긴 stale 백레퍼런스의 반복 순회). 회수 설계는 관측 맵의 죽은 엔트리 정리를 선행해야 함 |
| **OptLIBA.2690 / .2925** (관성 LBA 그래프 구성) | L2: 326객체 × **65,536회**, 중앙값 12~20s, 최대 142s | **내력벽** — 관성 윈도 구성이 오래 죽은 객체를 대량 반복 읽기 |
| **OptBA.272 / .509** (GBA 전수 스윕 가드) | n_ev == n_obj (439/439, 2,911/2,911 — 객체당 정확 1회) | **한정 스킵** — 만료-안전 후보 (스킵 가드는 isBad 판정 외 멤버 접근 없음; weak 만료 검사로 1:1 치환 가능) |
| OptSim3.2382 | 1회, delta ≈ 0 | 경합 창 (클래스 1과 동형 — 진입부 pin으로 폐쇄) |
| 나머지 56사이트 | 이 시나리오들에서 0건 (L2.report/L3.report의 zero-event 목록) | 후보 — 시나리오 추가(병합/멀티맵)로 승격 필요, 현 단계 판정 유보 |

## 음수 델타 주석

OptLBA.1290 min = −186 ops/−48ms, OptSim3 = −11ms: 프로브가 SetBadFlag의
스탬프보다 **먼저** 기록된 동시 실행 — mbBad 설정(객체 뮤텍스 내)과 스탬프
사이, 또는 스레드 간 링 기록 순서의 찰나. 의미상 delta ≈ 0(사망과 동시
읽기)로 해석한다. 이 창 자체가 클래스 1에서 실측한 "pin으로 닫히는 경합
창"의 재현이다.

## 계기 결함 이력 (본 캡처 이전에 수리됨)

- body-stealing 세그폴트: 중괄호 없는 `if(pMP)` 가드 아래 'before' 삽입
  5곳이 가드 몸통을 가로챔 → 프로브를 가드 위로 승격(`p && p->isBad()`).
  게이트 바이너리는 전 과정 md5 동일(증거: scratchpad bin_md5_class3).
- 섭동 게이트 티어-2 보정: 도커 자원 변경 후 비계측 팔 스스로가 전이
  집합을 바꿈(2연속) → 쌍 동일성은 이 모드에 검정력 부족, 골든 우주
  포함으로 보정. 게이트 방법론 이력의 v2.1/v2.2 검정력 교정과 같은 계열.

주의: 진단 전용 — ATE 판정 금지. 1런 캡처이며 처분 승격에는 반복 런 필요.
